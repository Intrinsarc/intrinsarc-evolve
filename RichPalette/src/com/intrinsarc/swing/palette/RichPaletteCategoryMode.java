package com.intrinsarc.swing.palette;

public enum RichPaletteCategoryMode
{
    AUTOMATIC,      // can be smaller if required, but will show as much as possible
    RESIZED,        // set to a given size by choice. can go smaller or larger
    MINIMIZED,      // locked at minimum
    MAXIMIZED,       // locked at maximum if possible
}
